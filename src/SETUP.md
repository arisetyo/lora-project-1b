# Setup — Modules to Copy from Phase 1A

Three modules are identical to Phase 1A and should be copied directly:

```bash
cp ../lora-project/src/lora.py     src/lora.py
cp ../lora-project/src/train.py    src/train.py
cp ../lora-project/src/evaluate.py src/evaluate.py
```

Only `data.py` and `ablation.py` are new/modified for Phase 1B.
